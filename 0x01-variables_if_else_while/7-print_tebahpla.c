#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: Invariably 0 (Feat)
 */

int main(void)
{
	char alpha_low;

	alpha_low = 'z';
	while
		(alpha_low >= 'a') {
			putchar(alpha_low);
			alpha_low--;
		}
	putchar('\n');
	return (0);
}
