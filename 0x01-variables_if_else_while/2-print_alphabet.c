#include <stdio.h>

/**
 * main - Starting point
 *
 * Return: Invariably 0 (Feat)
 */

int main(void)
{
	/* variable declaration */
	char alpha_low;

	alpha_low = 'a';
	while
		(alpha_low <= 'z') {
			putchar(alpha_low);
			alpha_low++;
		}
	putchar('\n');
	return (0);
}
