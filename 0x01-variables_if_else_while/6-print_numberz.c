#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: Invariably 0 (Feat)
 */

int main(void)
{
	int numz;

	numz = 0;
	while
		(numz < 10) {
			putchar(numz + '0');
			numz++;
		}
	putchar('\n');
	return (0);
}
